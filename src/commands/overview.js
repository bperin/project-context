// overview command — generates overview.csv from spec/plan/task Status sections
// TODO: read all spec/plan/task markdown files, parse ## Status sections,
// emit CSV with columns: Type, ID, Title, Status, Progress, Dependencies, Notes.
// See MASTER_PROMPT.md for the spec.

function overview(options) {
  console.log("overview: not yet implemented");
  console.log("See MASTER_PROMPT.md for the spec.");
}

module.exports = { overview };
